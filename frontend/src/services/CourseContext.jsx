import { createContext } from "react";

/* Context for holding the courses that are currently loaded */
/* This context is not yet used, but will be used for the map page */
export const CourseContext = createContext(null);